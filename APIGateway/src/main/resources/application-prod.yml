services:
  catalog: "${CATALOG_URL}"
  reservation: "${RESERVATION_URL}"
  expected book: "${EXPECTED_BOOK_URL}"
  event: "${EVENT_URL}"
  user: "${USER_URL}"
  auth: "${AUTH_URL}"
  frontend: "${FRONTEND_URL}"
  minio: "${MINIO_URL}"
jwt:
  secret: "${JWT_SECRET}"
spring:
  data:
    redis:
      repositories:
        enabled: false
      host: "${REDIS_HOST}"
      port: "${REDIS_PORT}"