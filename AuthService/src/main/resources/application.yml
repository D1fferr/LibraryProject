spring:
  application:
    name: AuthService
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  cache:
    type: redis

  mvc:
    hidden method:
      filter:
        enabled: true
  profiles:
    active: local
server:
  port: 8086